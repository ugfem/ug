{
    IBClasses = (
        {
            ACTIONS = {showHelp = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {
                appendText = id; 
                closeGraphic = id; 
                executeCommand = id; 
                newGraphic = id; 
                saveDocument = id; 
                showInfo = id; 
                showInspector = id; 
                showPreferences = id; 
            }; 
            CLASS = MAppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {inspector = id; shell = id; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}