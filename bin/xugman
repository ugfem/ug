#!/bin/sh
if test $# -ge 1 
then
	echo "usage: $0";
	exit 1;
fi


if test "x$UGROOT" = "x"
then
    echo "$0: to use $0 set shell environment variable UGROOT!";
    exit 1;
fi

# set man path 
MANPATH=$UGROOT/catman:$UGROOT/../diff2d/catman:$UGROOT/../cd3d/catman:$UGROOT/../fem/catman
export MANPATH

xman -notopbox -bothshown
